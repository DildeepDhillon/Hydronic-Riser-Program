class Schedule:

    def __init__(self, tag, model, flow):
                self.tag = tag
                self.model = model
                self.flow = flow

    # Code was for manual entry
    """        
    def __init__(self, tag, list1):
        for tuple1 in list1:
            if tuple1[0] == tag:
                self.tag = tag
                self.model = tuple1[1]
                self.flow = tuple1[2]
    """

